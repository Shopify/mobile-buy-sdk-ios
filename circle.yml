machine:
  xcode:
    version: "6.2"

test:
  override:
    - xctool
      -reporter pretty
      -reporter junit:$CIRCLE_TEST_REPORTS/xcode/results.xml
      -scheme "Mobile Buy SDK Tests"
      -project "Mobile Buy SDK/Mobile Buy SDK.xcodeproj"
      -sdk iphonesimulator8.2 clean test
