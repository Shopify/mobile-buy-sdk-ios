machine:
  xcode:
    version: "7.0"

test:
  override:
    - xctool
      -reporter pretty
      -reporter junit:$CIRCLE_TEST_REPORTS/xcode/results.xml
      -scheme "Mobile Buy SDK Tests"
      -workspace "Mobile Buy SDK.xcworkspace"
      -sdk iphonesimulator9.0 clean test

experimental:
    notify:
        branches:
            only: ["master", "develop"]
