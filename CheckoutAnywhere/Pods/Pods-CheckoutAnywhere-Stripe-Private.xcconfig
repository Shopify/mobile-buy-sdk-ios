#include "Pods-CheckoutAnywhere-Stripe.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Stripe" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Stripe"
OTHER_LDFLAGS = -ObjC ${PODS_CHECKOUTANYWHERE_STRIPE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}